<form>
    <h1 class="form-title">{{{$Me->getName()}}} > {{{$class}}}</h1>
    <div class="tab-base"></div>
    <div class="fieldholder CollectionFormField">
        <label for="jobs">Jobs</label>
        <div class="field">
            {{{$collection}}}
        </div>
    </div>
</form>